# Feature Matrix

This matrix tracks the Hyprspace-style feature-completeness goal against the
current Rust implementation. It is intentionally conservative: a row is marked
operational only when there is code, documentation, and a concrete verification
path.

## Status Key

- `operational`: implemented and covered by normal verification or a documented
  privileged end-to-end command.
- `partial`: implemented enough to use, but the evidence is narrower than the
  final goal or an important production concern remains.
- `blocked`: not currently possible through the selected dependency surface.

## Matrix

| Area | Status | Current evidence | Verification |
| --- | --- | --- | --- |
| Packet data plane | partial | `src/runtime/packet.rs`, `src/wire.rs`, and `src/runtime/runner.rs` implement a fixed binary packet frame over authenticated libp2p request-response streams. `src/config.rs`, `src/runtime/control.rs`, and `src/runtime/packet_plane.rs` expose owned packet-plane listener config, bind configured UDP listeners, advertise direct UDP endpoint candidates, validate socket and DNS-style `host:port` packet endpoint candidates, resolve DNS candidates to concrete socket endpoints before signed packet-plane negotiation, rank public-style packet endpoints ahead of private, loopback, link-local, and wildcard listener candidates during handshake selection, negotiate signed hello/accept handshakes over the authenticated control plane, derive directional ChaCha20-Poly1305 packet keys with ephemeral X25519 keys, register per-peer packet-plane sessions with endpoint/MTU/session metadata, seal/open encrypted datagram envelopes around the existing packet frame, reject replayed packet-plane datagrams for registered sessions, exchange sealed frames over bound UDP sockets, let outbound queue draining prefer an established packet-plane datagram session when peer capabilities and path selection allow it, send selected-MTU path probes over that datagram session, acknowledge packet-plane path probes, and accept inbound UDP frames from established sessions through the same route authorization/replay/TUN write path as stream packets. The packet-plane runtime also has a Quinn-based owned QUIC DATAGRAM primitive that sends and receives the same authenticated datagram envelope over an explicitly trusted QUIC connection. `network.packet_plane.quic_listen` binds one owned QUIC listener, `network.packet_plane.quic_external_endpoints` advertises QUIC-specific endpoint candidates separately from UDP packet endpoints, and daemon status/state/capability views expose the current QUIC listener, certificate byte count, and runtime sessions. Control capabilities include a validated DER certificate field for owned QUIC advertisements so peers can reject incomplete or oversized QUIC trust material before path selection. The control-plane hello flow now opens/accepts the owned Quinn connection before registering an owned QUIC packet-plane session, and inbound QUIC datagrams feed the same authorization, rate-limit, route, replay, path-probe, and TUN write path as UDP packet-plane datagrams. Outbound queue draining distinguishes owned UDP and owned QUIC datagram backends and prefers an established owned QUIC packet-plane session when the peer advertised owned QUIC support, falling back to an established UDP packet-plane session or stream fallback as needed. Control, service, daemon, and remote status distinguish generic datagram packet-path compatibility from exact native libp2p QUIC datagram support, exact owned UDP packet-plane support, and exact owned QUIC packet-plane support. `src/queue.rs` and `src/runtime/runner.rs` add bounded per-peer queues and flow-sharded stream fallback. Packet-plane session initiation waits for an existing direct libp2p transport path so relayed-only peers do not bypass relay fallback, and direct path establishment/promotion retriggers packet-plane hello negotiation when remote capabilities were learned earlier over a relayed path. Packet-plane receive rejections are logged with stable names and counted by specific datagram/session/parser reason in runtime metrics. Recorded namespace E2E smoke evidence on 2026-08-04 proves packet forwarding over direct, discovery-assisted, relay-assisted, invite-assisted, hole-punch-promoted UDP/libp2p paths, and a focused direct owned-QUIC packet-plane path. | `nix develop -c cargo test`; focused: `nix develop -c cargo test runtime::packet_plane::tests`; endpoint ranking: `nix develop -c cargo test packet_plane_endpoint_selection`; negotiation: `nix develop -c cargo test runtime::runner::tests::packet_plane_control_negotiation_establishes_sessions`; owned QUIC negotiation: `nix develop -c cargo test runtime::runner::tests::packet_plane_control_negotiation_establishes_quic_sessions`; promotion retrigger: `nix develop -c cargo test runtime::runner::tests::direct_path_promotion_retries_packet_plane_negotiation_with_cached_capabilities`; owned QUIC status: `nix develop -c cargo test runtime::runner::tests::current_packet_plane_quic_snapshot_reports_runtime_sessions`; path probes: `nix develop -c cargo test path_probe`; outbound UDP daemon integration: `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_prefers_established_packet_plane_datagram_path`; outbound QUIC daemon integration: `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_prefers_established_packet_plane_quic_datagram_path`; inbound daemon integration: `nix develop -c cargo test runtime::runner::tests`; owned QUIC primitive/runtime snapshot: `nix develop -c cargo test packet_plane_quic`; owned QUIC capability certificate: `nix develop -c cargo test runtime::control::tests`; backend capability model: `nix develop -c cargo test packet_datagram_backends`; owned QUIC namespace E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_owned_quic_packet_plane -- --ignored --exact --nocapture`; full namespace E2E: `nix run .#tun-e2e -- -- --ignored --nocapture` |
| Native QUIC datagrams | blocked | `src/runtime/runner.rs` advertises native libp2p QUIC datagrams as unsupported because the locked `libp2p-quic`/`Swarm` surface does not expose an application datagram sender/receiver. Owned UDP packet-plane datagrams are reported separately as `supports_owned_udp_packet_plane`, and the protocol reserves `supports_owned_quic_packet_plane` for the owned Quinn backend, so the daemon does not conflate implemented owned UDP, owned QUIC forwarding, and unavailable native libp2p QUIC datagrams. | `nix develop -c cargo test runtime::runner::tests::local_packet_data_plane_is_identity_keyed_stream_fallback_only`; backend capability model: `nix develop -c cargo test packet_datagram_backends`; owned QUIC primitive: `nix develop -c cargo test runtime::packet_plane::tests::quic_runtime_sends_encrypted_frame_to_registered_peer`; owned QUIC negotiation: `nix develop -c cargo test runtime::runner::tests::packet_plane_control_negotiation_establishes_quic_sessions`; owned QUIC queue drain: `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_prefers_established_packet_plane_quic_datagram_path` |
| Stream fallback | operational | The fallback uses identity-keyed request-response streams, per-peer send windows, and flow shards so same-shard packets stay queued while unrelated shards can drain. | `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_gates_stream_fallback_by_flow_shard` |
| Queueing and backpressure | operational | `src/queue.rs` enforces packet count, byte count, packet age, per-peer fairness, expired drops, and blocked-peer retention. Runtime metrics expose queue drops and blocked reasons. | `nix develop -c cargo test queue::tests` |
| Control, packet, and service surfaces | operational | Separate codecs and behaviours exist in `src/runtime/control.rs`, `src/runtime/packet.rs`, and `src/runtime/service.rs`. Capability and service status exchanges are separate from packet forwarding. | `nix develop -c cargo test runtime::p2p::tests::two_nodes_exchange_packet_request`; full codec coverage: `nix develop -c cargo test` |
| Route ownership and source authorization | operational | `src/route.rs` and `src/runtime/forward.rs` authorize advertised prefixes, local sources, inbound sources, and local destinations. Capability validation rejects unauthorized advertised routes. | `nix develop -c cargo test route::tests`; focused: `nix develop -c cargo test runtime::runner::tests::capability_response_rejects_unauthorized_route_advertisements` |
| Membership and invite UX | partial | `src/invite.rs`, `src/config.rs`, and CLI commands implement signed invite export/import, membership tags, previous-tag compatibility, expiry, signature verification, config generation, bootstrap preservation, relay reservation preservation, relay-assisted inviter addresses, and malformed relay reservation rejection. `src/membership.rs` adds signed membership records that bind issuer/member peer IDs to identity public keys, membership epoch, sequence, roles, optional route grants, optional expiry, revocation tombstones, and network name; runtime config validation accepts valid records and rejects tampered, expired, wrong-network, wrong-key, authority-bearing revocations, expiring revocations, or malformed-route records. Valid local records now feed the operational authorization model: `overlay_member` admits record-only peers for overlay connection, packet authorization, builtin overlay host routes, and discovery/dial gates, while `route_authority` grants additional advertised route prefixes for the latest valid overlay-member record. The CLI now exports public identity files, issues grant or revocation records for public subjects without requiring the member private key, verifies records, installs verified records into config idempotently by newest epoch/sequence, reports revocation state, and automatically assigns `route_authority` when issuing route grants. Authenticated control capabilities exchange a bounded set of signed records between configured overlay peers; received records are verified against issuer peer IDs already trusted from local `network.member_records`, merged by latest epoch/sequence, capped, ignored when stale, pruned when expired by runtime maintenance, and accepted records update live overlay membership, packet authorization, and route-grant ownership. Newer revocation records remove record-derived live overlay membership and packet/route authorization, while newer grants can re-admit the member. Kademlia-enabled nodes also publish and fetch bounded signed membership-record bundles under overlay-scoped value-record keys for current and previous membership scopes; fetched bundles are size-limited, versioned, network/scope-checked, and still merged only through the same local issuer-trust and signature validation path. Further work remains for richer multi-issuer lifecycle policy UX and live public-DHT propagation evidence. | `nix develop -c cargo test invite::tests`; membership records: `nix develop -c cargo test membership_record`; membership CLI: `nix develop -c cargo test membership_record_issue`; dynamic exchange: `nix develop -c cargo test capability_response_learns_member_records`; DHT record distribution: `nix develop -c cargo test kademlia_membership_record`; config validation/routes: `nix develop -c cargo test member_record`; relay-assisted invites: `nix develop -c cargo test signed_invite_round_trips_relay_assisted_reachability`; focused: `nix develop -c cargo test runtime::runner::tests::capability_response_accepts_previous_membership_tag` |
| Replay and session expiry | operational | `src/runtime/forward.rs` keeps per-peer/session replay windows, bounds total replay windows, expires stale sessions, and exposes replay window count in daemon state. `src/runtime/packet_plane.rs` also rejects duplicate and too-old authenticated UDP datagrams for registered packet-plane sessions before forwarding, while bounding replay windows per packet-plane peer session with a validated config limit. `src/config.rs` exposes the packet-plane session TTL with a 600-second default and the replay-window limit with a 1024-window default, rejecting zero values for both. `src/runtime/packet_plane.rs` timestamps owned UDP packet-plane sessions, and `src/runtime/runner.rs` expires them after the configured lifetime, marks the direct datagram path unhealthy, records expiry metrics, and attempts renegotiation where this node is the deterministic initiator. | `nix develop -c cargo test runtime::forward::tests::replay_window_expires_after_session_ttl`; packet-plane replay: `nix develop -c cargo test runtime::packet_plane::tests::udp_runtime_rejects_replayed_registered_session_datagram`; packet-plane replay bounds: `nix develop -c cargo test runtime::packet_plane::tests::packet_plane_session_replay_windows_are_bounded_per_peer_session`; packet-plane expiry: `nix develop -c cargo test packet_plane_session_expiry_marks_datagram_path_unhealthy`; config validation: `nix develop -c cargo test packet_plane_config`; full replay coverage: `nix develop -c cargo test runtime::forward::tests` |
| Resource limits and rate limits | operational | `src/config.rs`, `src/runtime/p2p.rs`, and `src/runtime/runner.rs` expose swarm connection limits, stream limits, relay limits, and per-peer inbound packet rate limiting. | `nix develop -c cargo test runtime::runner::tests::peer_packet_rate_limiter_caps_each_peer_independently`; full config coverage: `nix develop -c cargo test config::tests` |
| Path selection and promotion | operational | `src/path.rs` scores direct datagram, direct stream, TCP, and relay paths. Packet-plane path probes carry a bounded random token in the probe/ACK payload, record observed RTT for the selected datagram path, expose that RTT in daemon path/state views, and let high observed latency affect later path scoring. Runtime records promotions/fallbacks, logs path-selection changes, demotes selected packet-plane datagram paths on send-side transport/session failures so later queue draining can fall back to the next usable stream or relay path, and immediately dials the peer's configured/discovered addresses to recover a direct packet-plane path. | `nix develop -c cargo test path::tests`; path probes: `nix develop -c cargo test path_probe`; daemon path output: `nix develop -c cargo test runtime_control_view_lines_report_peers_routes_paths_and_capabilities`; demotion: `nix develop -c cargo test packet_plane_transport_failure_demotes_datagram_path_to_relay`; recovery targets: `nix develop -c cargo test packet_plane_recovery_targets` |
| NAT/DCUtR hole punching | operational on supported Linux hosts | The privileged namespace test starts a relay plus two peers, uses relay fallback, enables DCUtR and AutoNAT, observes a successful hole-punch result, promotes to a direct TCP path, negotiates packet-plane sessions only after a direct transport path exists, and verifies TUN traffic over packet-plane datagrams. A recorded namespace smoke on 2026-08-04 passed the relay-to-direct promotion case as part of the full seven-test namespace suite. | `nix run .#tun-e2e -- tun_namespace_relay_overlay_promotes_to_direct_path -- --ignored --exact --nocapture`; full evidence: `docs/namespace-e2e-smoke.md` |
| Discovery and public libp2p/IPFS bootstrap | partial | Private Kademlia, mDNS, configurable bootstrap peers, relay reservations, a `--relay-peer PEER_ID=MULTIADDR` infrastructure shortcut, an opt-in IPFS Kademlia protocol/bootstrap template, libp2p RSA peer identity decoding for legacy public IPFS bootstrap IDs, and rootless `bootstrap-check`/`relay-scan`/`relay-check` diagnostics exist. Configured bootstrap peers, relay reservation peers, and relays embedded in configured relayed peer addresses are admitted as reachability infrastructure separately from overlay VPN membership, and public peers are not route authority. The relay shortcut adds the relay to bootstrap/AutoNAT reachability peers and creates a matching `/p2p-circuit` reservation without adding the relay to the VPN peer list. Bootstrap, configured peer, and relay-reservation addresses are registered as AutoNAT probe servers when AutoNAT is enabled. Membership-protected overlays scope Kademlia provider records by the derived membership tag, while no-membership overlays keep the network-name key for compatibility. Kademlia refresh also publishes retained signed membership records to separate overlay-scoped value-record keys when provider advertisement is enabled, and fetches current, previous-tag, and untagged membership-record keys for trust-filtered merging. During membership-key rotation, refreshes query previous-tag rendezvous keys without advertising old tags. Provider results from public or private DHT routing are counted, but only configured VPN peers become dial candidates; ignored providers are exposed as runtime metrics. Provisional relay-only infrastructure peers discovered through public routing are removed after asynchronous dial failures before connection, so failed public DHT hints do not remain admitted indefinitely. Automatic relay reservation policy is configurable through `network.relay.auto` and `init-config` flags for retained candidates, concurrent reservations, and retry delay. `bootstrap-check` builds the same libp2p host without TUN, starts bootstrap/Kademlia/AutoNAT setup, reports per-bootstrap-peer reachability with any/all success thresholds, reports observed AutoNAT status, reports DCUtR enablement/readiness/success/direct-connection counters, reports per-relay reservation acceptance/relayed-listen readiness, clears relay readiness when relayed listen addresses expire or final relay connections close, reports per-configured-relayed-peer circuit dialing status, can write the same state as JSON, and can require configured relay reservations, an observed non-unknown AutoNAT status, rootless DCUtR relay readiness, successful DCUtR hole punching plus a direct non-relayed peer connection, and configured relayed peer circuits to become operational. `relay-scan` dials configured or explicit bootstrap peers directly; with bundled public IPFS bootstrap peers it also enables `/ipfs/kad/1.0.0`, samples peers learned through the public routing table, reports bootstrap/total/routing peer counts, filters unsupported-transport candidate addresses, prints bounded direct TCP/QUIC relay candidate multiaddrs for peers that advertise the circuit-relay v2 hop protocol, can validate scanned candidates immediately in round-robin relay-peer order, and can write a default or preserved-overlay relay-assisted config from the first validated scanned relay. `relay-check` accepts public relay candidates directly, creates temporary local probe nodes, validates reservation plus relayed circuit dialing by default, can require a DCUtR success event and direct non-relayed post-punch connection, stops after the first usable relay, reports candidate-level failures, and can write the same default relay-assisted config from the first usable relay. The packaged `public-relay-repro` command preserves scan, relay-circuit, DCUtR, host-network, replay-command, and two-host handoff artifacts, including a Host A listener report with relay connection, reservation, relayed-listen-address, listener-address, and error evidence, and `docs/network-debugging.md` now has a repeatable checklist for comparing those artifacts across NATs and hosts. Ignored live smokes can validate the same supplied public relay and public-relay DCUtR path through `P2P_VPN_LIVE_RELAY_MULTIADDRS` with a tunable per-candidate timeout. Recorded live rootless smoke checks on 2026-08-04 connected to all five default public IPFS bootstrap peers, registered five AutoNAT probe servers, observed AutoNAT `private` status, tracked public Kademlia routing peers, proved a Kademlia-learned public relay reservation and relayed circuit, and wrote a runtime-valid relay-assisted config; public DCUtR still timed out from this host; see `docs/public-bootstrap-smoke.md`. mDNS and DHT namespace tests now verify discovery, capability exchange, packet-plane endpoint advertisement, signed packet-plane session negotiation, and real datagram packet forwarding after discovery. A relay-assisted signed-invite namespace test now proves an imported invite can carry the inviter's relayed address into a runnable overlay that exchanges capabilities and forwards TUN traffic over circuit relay. | `nix develop -c cargo test runtime::p2p::tests::build_node_accepts_ipfs_compatible_kademlia_protocol`; auto-relay policy: `nix develop -c cargo test auto_relay`; rootless bootstrap/relay/AutoNAT/DCUtR/relayed-peer-circuit diagnostic: `nix develop -c cargo test bootstrap_check`; bootstrap JSON report: `nix develop -c cargo test bootstrap_check_writes_machine_readable_report`; public relay packaged repro: `nix run .#public-relay-repro`; public relay scan: `nix develop -c cargo run -- relay-scan --ipfs-bootstrap-peers --timeout-seconds 30`; widened public relay scan: `nix develop -c cargo run -- relay-scan --ipfs-bootstrap-peers --timeout-seconds 45 --max-candidates 64`; public relay CLI probe: `nix develop -c cargo run -- relay-check --relay-candidate /dns4/relay.example.net/tcp/4001/p2p/RELAY --timeout-seconds 45` and `nix develop -c cargo run -- relay-check --relay-candidate /dns4/relay.example.net/tcp/4001/p2p/RELAY --require-dcutr-success --timeout-seconds 45`; relay config output: `nix develop -c cargo test public_relay_config_args`; configured infrastructure split: `nix develop -c cargo test runtime::runner::tests::overlay_membership_separates_vpn_peers_from_configured_infrastructure`; live public relay smoke harnesses: `P2P_VPN_LIVE_RELAY_TIMEOUT_SECONDS=90 P2P_VPN_LIVE_RELAY_MULTIADDRS=/dns4/relay.example.net/tcp/4001/p2p/RELAY nix develop -c cargo test runtime::bootstrap_check::tests::bootstrap_check_can_probe_live_public_relayed_peer_circuit -- --ignored --exact --nocapture` and `P2P_VPN_LIVE_RELAY_TIMEOUT_SECONDS=90 P2P_VPN_LIVE_RELAY_MULTIADDRS=/dns4/relay.example.net/tcp/4001/p2p/RELAY nix develop -c cargo test runtime::bootstrap_check::tests::bootstrap_check_can_probe_live_public_dcutr_success -- --ignored --exact --nocapture`; public IPFS + AutoNAT smoke: `nix develop -c cargo run --quiet -- init-config --output /tmp/p2p-vpn-public/p2p-vpn.json --ipfs-kademlia --ipfs-bootstrap-peers --disable-mdns --disable-kademlia-provider-advertisement --force` then `nix develop -c cargo run --quiet -- bootstrap-check --config /tmp/p2p-vpn-public/p2p-vpn.json --timeout-seconds 45 --require-autonat-status --write-report /tmp/p2p-vpn-public/bootstrap-check.json --force`; relay shortcut: `nix develop -c cargo test relay_peer_shortcut`; rendezvous scoping: `nix develop -c cargo test kademlia_rendezvous_key`; membership-record DHT: `nix develop -c cargo test kademlia_membership_record`; rotation lookup: `nix develop -c cargo test kademlia_refresh_queries_previous_membership_rendezvous_keys`; provider filtering: `nix develop -c cargo test runtime::runner::tests::kademlia_provider_results_dial_configured_peers_and_count_failures`; AutoNAT relay servers: `nix develop -c cargo test runtime::p2p::tests::build_node_registers_relay_reservations_as_autonat_servers`; invite relay E2E: `nix run .#tun-e2e -- tun_namespace_invite_import_crosses_relay_overlay -- --ignored --exact --nocapture`; mDNS E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_mdns_discovered_overlay -- --ignored --exact --nocapture`; DHT E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_dht_discovered_overlay -- --ignored --exact --nocapture` |
| MTU and MSS behavior | operational | Effective packet MTU is bounded by the wire payload length, packet-plane UDP envelope ceiling, and peer/path MTU. Packet-plane sealing rejects payloads that would exceed the safe UDP datagram ceiling before socket send. Linux route commands include MTU and `advmss` hints when possible. Oversized path-MTU drops generate local IPv4 fragmentation-needed or ICMPv6 packet-too-big feedback when the original packet is parseable, outbound oversized drops lower the selected path's MTU estimate to the proven negotiated ceiling, and packet-plane path probes exercise the selected datagram path with ACK-backed upward probe search capped by authenticated peer capability and session MTU. Metrics distinguish generic path-MTU updates from ACK-confirmed path-MTU probe raises, expose path-probe ACK sends, and classify packet-too-big feedback as emitted, skipped because no TUN writer is available, skipped because the original packet is unparseable, or failed during write. Configured, live-peer, and daemon MTU views explicitly report that overlay fragmentation is disabled. | `nix develop -c cargo test runtime::tun::tests::packet_too_big_builds_ipv4_fragmentation_needed`; packet-too-big feedback outcomes: `nix develop -c cargo test runtime::runner::tests::packet_too_big_feedback_records_no_writer_and_unparseable_outcomes`; packet-plane ceiling: `nix develop -c cargo test packet_plane_datagram_rejects_payload_above_udp_safe_ceiling`; path MTU learning: `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_respects_peer_capability_mtu`; policy output: `nix develop -c cargo test mtu_lines_configured_report_route_mss_and_path_estimates`; `nix develop -c cargo test peer_live_mtu_lines_report_negotiated_path_ceiling`; `nix develop -c cargo test runtime_mtu_lines_report_overlay_fragmentation_policy`; path probes: `nix develop -c cargo test path_probe`; full MTU coverage: `nix develop -c cargo test mtu` |
| Daemon lifecycle | operational | `src/runtime/runner.rs` handles shutdown reasons, structured runtime logs, metrics snapshots, control-socket shutdown, and orderly cleanup. | `nix develop -c cargo test runtime::runner::tests::runtime_control_shutdown_acknowledges_and_requests_stop`; full lifecycle coverage: `nix develop -c cargo test runtime::runner::tests` |
| Local daemon control CLI | operational | `src/runtime/control_socket.rs` and `src/main.rs` expose `daemon-status`, `daemon-state`, `daemon-peers`, `daemon-routes`, `daemon-paths`, `daemon-mtu`, `daemon-capabilities`, and `daemon-shutdown`. Status and state output include the configured packet-plane session TTL and replay-window limit alongside listener/session counts. `daemon-state` also reports relay-only infrastructure peers admitted from Kademlia/public reachability discovery, including their candidate address and whether the swarm is currently connected. Daemon view commands preserve line-oriented text by default and accept `--format json` for a stable schema-versioned envelope that namespace repros save beside text snapshots. | `nix develop -c cargo test runtime::control_socket::tests::control_socket_serves_daemon_view_requests`; TTL/limit and relay-infrastructure line coverage: `nix develop -c cargo test runtime_state_lines_include_peer_capabilities_paths_and_probes`; daemon view JSON: `nix develop -c cargo test daemon_view_json`; namespace artifacts: `nix develop -c cargo test daemon_snapshot_capture_records_missing_control_sockets` |
| Remote status/control CLI | operational | `src/runtime/remote.rs` and `src/main.rs` query peers for service status, live paths, routes, capabilities, MTU-related state, reported selected-path kind/score/MTU/RTT, reported packet-plane session TTL/replay-window limit, and packet-plane payload/overhead limits without requiring daemon-local socket access. Older service responses without those fields decode as unknown. | `nix develop -c cargo test runtime::remote::tests::query_peer_status_exchanges_live_control_and_service_status`; service compatibility: `nix develop -c cargo test status_response_decodes_missing_packet_plane_limits_as_unknown`; service selected path: `nix develop -c cargo test service_status_response_accepts_configured_compatible_peers`; CLI line coverage: `nix develop -c cargo test peer_live` |
| Security audit logging | operational | `src/runtime/runner.rs` emits structured audit fields for rejected packets without logging packet payload bytes. Metrics classify rejection reasons. | `nix develop -c cargo test runtime::runner::tests::packet_rejection_audit_fields_include_safe_packet_metadata` |
| NixOS integration | operational | `nix/nixos-module.nix` defines instances, systemd units, runtime directories, firewall ports, TUN kernel module, control socket shutdown, restart behavior, and conservative systemd hardening. The service keeps only `CAP_NET_ADMIN`/`CAP_NET_RAW`, allows `/dev/net/tun`, restricts device access and address families to VPN needs, uses a private `/tmp`, protects host/kernel mutation surfaces, and applies read-only host filesystem protection. The VM smoke check builds a generated config into a NixOS guest, imports the module, starts `p2p-vpn-smoke.service`, waits for the daemon control socket, verifies status metrics through `daemon-status`, stops the service through systemd, and confirms socket cleanup plus a successful unit result. | `nix build .#checks.x86_64-linux.nixos-module`; `nix build .#checks.x86_64-linux.nixos-vm-smoke`; full gate: `nix flake check` |
| Release packaging | operational | `flake.nix` builds the binary package and reproducible release archive with README, flake files, NixOS module, examples, and this matrix. The `releaseArchiveSanity` check unpacks the archive, rejects unsafe paths, verifies the expected operational files are present, and confirms the archived CLI can print help. | `nix build .#releaseArchive`; `system=$(nix eval --raw --impure --expr builtins.currentSystem); nix build ".#checks.${system}.releaseArchiveSanity"`; full gate: `nix flake check` |

## Remaining Non-Trivial Gaps

1. Native QUIC datagrams are not operational through the current libp2p
   dependency surface. Closing this requires a lower-level transport integration,
   a dependency upgrade that exposes application datagrams, or a different
   datagram-capable substrate. The control and service protocols have an exact
   `supports_owned_quic_packet_plane` bit plus validated certificate
   advertisement, and the packet-plane runtime now has a Quinn QUIC DATAGRAM
   primitive. The daemon can bind and advertise an owned QUIC listener, negotiate
   the Quinn connection and signed packet-plane session, drain queued packets
   over owned QUIC sessions, accept inbound owned QUIC datagrams, and pass a
   focused namespace E2E that forwards TUN traffic over an owned QUIC packet
   plane.
   Owned packet-plane listener configuration,
   listener binding, endpoint capability advertisement, automatic signed packet
   session negotiation over the control plane, X25519 key agreement, per-peer
   packet-plane session state, encrypted datagram frame primitives, socket-level
   UDP send/receive primitives, daemon outbound queue sends over an established
   packet-plane session, and daemon inbound handling for established
   packet-plane sessions exist.
2. Public libp2p/IPFS infrastructure is supported as discovery and reachability
   assistance, and public bootstrap reachability can now be checked with a
   rootless diagnostic that also covers configured relay reservation acceptance,
   AutoNAT status results, DCUtR relay readiness, configured relayed peer circuit
   dialing, and observed DCUtR success events. `relay-scan` can discover
   relay-hop candidate hints from configured or explicit bootstrap peers, and
   with bundled public IPFS bootstrap peers it now actively samples additional
   peers from the public `/ipfs/kad/1.0.0` routing table while filtering
   unsupported transport addresses, and its bounded candidate set prefers
   distinct relay peers when a newly identified relay can replace duplicate
   addresses from an already represented relay. It can also write the ordered
   direct relay candidate set as newline-separated multiaddrs for repeatable
   follow-up `relay-check --relay-candidates-file` runs. `relay-check` can
   combine that file with explicit `--relay-candidate` flags before applying
   the same ordering, host reachability filtering, and validation limits.
   `relay-scan --check-candidates` can
   immediately test scanned candidates with the same reservation/circuit/DCUtR
   proof path as `relay-check`, while both scan validation and manual
   `relay-check` skip IPv4-only or IPv6-only validation candidates on hosts
   without a usable route for that address family. Both validation paths can
   bound how many host-reachable candidates a validation pass probes. Each validated candidate
   uses one end-to-end timeout budget across relay reservation setup and the
   subsequent relayed-circuit or DCUtR proof. Validation preserves relay-peer
   diversity and tries QUIC-capable addresses before TCP alternates within each
   relay peer. Recorded 2026-08-04 public smokes now prove
   public Kademlia-assisted relay discovery, public relay reservation
   acceptance, relayed circuit dialing, and relay-assisted config generation
   against a routing-table relay candidate. A follow-up `--require-dcutr-success`
   run against that relay connected the relayed circuit but timed out during the
   direct QUIC hole-punch attempt. Bootstrap and relay-check reports now expose
   a stable public relay candidate `failure_stage` plus the most recent libp2p
   DCUtR failure as `dcutr last_error`, so future public relay runs can
   distinguish setup, reservation, relayed-circuit, DCUtR prerequisite, and
   direct handshake failures. `relay-scan --write-report` persists scan
   discovery as JSON with timeout knobs, public routing counts, peer identify
   results, relay-hop capability, dial failures, and candidate addresses before
   validation starts, so failed public scans still leave a reproducible
   artifact. `relay-check --write-report` also persists the bounded public
   relay probe as JSON with mode, timeout, validation cap, host-reachable
   candidates, skipped candidates, per-candidate failure stages, errors, and
   bootstrap/DCUtR summary fields, including observed relayed and direct
   connection addresses, for candidates that reached the bootstrap-check phase.
   `relay-check --config BASE --write-config OUTPUT`
   can preserve an existing overlay config while adding the first validated
   public relay as bootstrap/AutoNAT/relay infrastructure. `relay-check` can
   also consume larger bounded `relay-scan --write-candidates` artifacts when a
   validation cap is set, so broad public scans can feed repeatable smaller
   proof runs without manual file trimming. The packaged `public-relay-repro`
   app now runs discovery,
   relay-circuit validation, and DCUtR validation as separate phases, preserving
   the scan, relay-check, and DCUtR JSON reports even when a later phase fails.
   Relay-check JSON reports include per-candidate elapsed milliseconds and
   observed path addresses so timeout signatures and direct-promotion evidence
   can be compared across hosts. `relay-dcutr-listen` and `relay-dcutr-dial`
   now provide a two-host public DCUtR handoff: Host A reserves a selected
   public relay and writes a listener descriptor plus listener report, while Host B consumes that
   descriptor and writes a bootstrap/DCUtR dial report. The repro summary records
   per-phase elapsed seconds, per-report candidate elapsed ranges, and observed
   relayed/direct connection address counts so public topology failures can be
   triaged without hand-parsing each JSON artifact.
   The repro writes a runnable relay-assisted config when the relay-circuit phase
   succeeds, and the packaged `public-vpn-repro` command turns that config into
   two-host daemon start, health-gate, status/path/metrics collection, ping, and
   shutdown scripts. When `P2P_VPN_REPRO_BASE_CONFIG` is set, that
   relay-assisted config preserves the supplied overlay.
   `P2P_VPN_REPRO_CANDIDATES_FILE` can point at a previous candidate file to
   skip public discovery and immediately replay relay-circuit/DCUtR validation,
   which shortens repeated debugging on the same public relay set. Public
   topology failures can be compared across hosts while still preserving the
   strongest usable fallback found. A
   report-driven live run on 2026-08-04 found one public relay
   candidate that accepted a reservation, carried a relayed circuit, and wrote
   a relay-assisted config; the paired DCUtR-required report tested eight
   host-reachable candidates, with four reservation timeouts and four
   direct-dial timeouts after relayed-circuit setup. A packaged repro run on
   2026-08-05 again proved public IPFS bootstrap discovery, one public
   relay-circuit success, relay-assisted config generation, and four
   DCUtR-stage relayed circuits; it also recorded one non-relayed private
   `/ip4/192.168.0.180/...` direct connection, but no libp2p DCUtR success
   event. The long-running daemon also reports currently admitted relay-only
   infrastructure peers in
   `daemon-state`, including the candidate address and live swarm connection
   flag, so public DHT/IPFS reachability attempts are inspectable without
   scraping logs. The project still needs recorded public-relay-assisted DCUtR
   success from this host or another suitable NAT topology. Public
   infrastructure is not, and should not become, membership or route authority.
3. Privileged namespace tests passed on this host on 2026-08-04, including a
   focused owned-QUIC packet-plane path, but they still
   require a Linux kernel with user namespaces, network namespaces, veth, and
   `/dev/net/tun`. `nix run .#namespace-preflight` now checks those host
   prerequisites before running the slower ignored namespace suite, and
   `nix run .#tun-e2e` runs the same preflight by default.
