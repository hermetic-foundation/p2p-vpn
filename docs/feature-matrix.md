# Feature Matrix

This matrix tracks the Hyprspace-style feature-completeness goal against the
current Rust implementation. It is intentionally conservative: a row is marked
operational only when there is code, documentation, and a concrete verification
path.

## Status Key

- `operational`: implemented and covered by normal verification or a documented
  privileged end-to-end command.
- `partial`: implemented enough to use, but the evidence is narrower than the
  final goal or an important production concern remains.
- `blocked`: not currently possible through the selected dependency surface.

## Matrix

| Area | Status | Current evidence | Verification |
| --- | --- | --- | --- |
| Packet data plane | partial | `src/runtime/packet.rs`, `src/wire.rs`, and `src/runtime/runner.rs` implement a fixed binary packet frame over authenticated libp2p request-response streams. `src/config.rs`, `src/runtime/control.rs`, and `src/runtime/packet_plane.rs` expose owned packet-plane listener config, bind configured UDP listeners, advertise direct endpoint candidates, validate socket and DNS-style `host:port` packet endpoint candidates, resolve DNS candidates to concrete socket endpoints before signed packet-plane negotiation, rank public-style packet endpoints ahead of private, loopback, link-local, and wildcard listener candidates during handshake selection, negotiate signed hello/accept handshakes over the authenticated control plane, derive directional ChaCha20-Poly1305 packet keys with ephemeral X25519 keys, register per-peer packet-plane sessions with endpoint/MTU/session metadata, seal/open encrypted datagram envelopes around the existing packet frame, reject replayed packet-plane datagrams for registered sessions, exchange sealed frames over bound UDP sockets, let outbound queue draining prefer an established packet-plane datagram session when peer capabilities and path selection allow it, send selected-MTU path probes over that datagram session, acknowledge packet-plane path probes, and accept inbound UDP frames from established sessions through the same route authorization/replay/TUN write path as stream packets. `src/queue.rs` and `src/runtime/runner.rs` add bounded per-peer queues and flow-sharded stream fallback. Packet-plane session initiation waits for an existing direct libp2p transport path so relayed-only peers do not bypass relay fallback. Packet-plane receive rejections are logged with stable names and counted by specific datagram/session/parser reason in runtime metrics. | `nix develop -c cargo test`; focused: `nix develop -c cargo test runtime::packet_plane::tests`; endpoint ranking: `nix develop -c cargo test packet_plane_endpoint_selection`; negotiation: `nix develop -c cargo test runtime::runner::tests::packet_plane_control_negotiation_establishes_sessions`; path probes: `nix develop -c cargo test path_probe`; outbound daemon integration: `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_prefers_established_packet_plane_datagram_path`; inbound daemon integration: `nix develop -c cargo test runtime::runner::tests`; direct packet-plane E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_two_node_overlay -- --ignored --exact --nocapture`; mDNS packet-plane E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_mdns_discovered_overlay -- --ignored --exact --nocapture`; DHT packet-plane E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_dht_discovered_overlay -- --ignored --exact --nocapture`; promoted packet-plane E2E: `nix run .#tun-e2e -- tun_namespace_relay_overlay_promotes_to_direct_path -- --ignored --exact --nocapture` |
| Native QUIC datagrams | blocked | `src/runtime/runner.rs` advertises local QUIC datagrams as unsupported because the locked `libp2p-quic`/`Swarm` surface does not expose an application datagram sender/receiver. Datagrams are modelled as a preferred path kind, but packets are not falsely reported as datagram-sent. | `nix develop -c cargo test runtime::runner::tests::local_packet_data_plane_is_identity_keyed_stream_fallback_only` |
| Stream fallback | operational | The fallback uses identity-keyed request-response streams, per-peer send windows, and flow shards so same-shard packets stay queued while unrelated shards can drain. | `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_gates_stream_fallback_by_flow_shard` |
| Queueing and backpressure | operational | `src/queue.rs` enforces packet count, byte count, packet age, per-peer fairness, expired drops, and blocked-peer retention. Runtime metrics expose queue drops and blocked reasons. | `nix develop -c cargo test queue::tests` |
| Control, packet, and service surfaces | operational | Separate codecs and behaviours exist in `src/runtime/control.rs`, `src/runtime/packet.rs`, and `src/runtime/service.rs`. Capability and service status exchanges are separate from packet forwarding. | `nix develop -c cargo test runtime::p2p::tests::two_nodes_exchange_packet_request`; full codec coverage: `nix develop -c cargo test` |
| Route ownership and source authorization | operational | `src/route.rs` and `src/runtime/forward.rs` authorize advertised prefixes, local sources, inbound sources, and local destinations. Capability validation rejects unauthorized advertised routes. | `nix develop -c cargo test route::tests`; focused: `nix develop -c cargo test runtime::runner::tests::capability_response_rejects_unauthorized_route_advertisements` |
| Membership and invite UX | operational | `src/invite.rs`, `src/config.rs`, and CLI commands implement signed invite export/import, membership tags, previous-tag compatibility, expiry, signature verification, config generation, bootstrap preservation, relay reservation preservation, relay-assisted inviter addresses, and malformed relay reservation rejection. | `nix develop -c cargo test invite::tests`; relay-assisted invites: `nix develop -c cargo test signed_invite_round_trips_relay_assisted_reachability`; focused: `nix develop -c cargo test runtime::runner::tests::capability_response_accepts_previous_membership_tag` |
| Replay and session expiry | operational | `src/runtime/forward.rs` keeps per-peer/session replay windows, bounds total replay windows, expires stale sessions, and exposes replay window count in daemon state. `src/runtime/packet_plane.rs` also rejects duplicate and too-old authenticated UDP datagrams for registered packet-plane sessions before forwarding, while bounding replay windows per packet-plane peer session with a validated config limit. `src/config.rs` exposes the packet-plane session TTL with a 600-second default and the replay-window limit with a 1024-window default, rejecting zero values for both. `src/runtime/packet_plane.rs` timestamps owned UDP packet-plane sessions, and `src/runtime/runner.rs` expires them after the configured lifetime, marks the direct datagram path unhealthy, records expiry metrics, and attempts renegotiation where this node is the deterministic initiator. | `nix develop -c cargo test runtime::forward::tests::replay_window_expires_after_session_ttl`; packet-plane replay: `nix develop -c cargo test runtime::packet_plane::tests::udp_runtime_rejects_replayed_registered_session_datagram`; packet-plane replay bounds: `nix develop -c cargo test runtime::packet_plane::tests::packet_plane_session_replay_windows_are_bounded_per_peer_session`; packet-plane expiry: `nix develop -c cargo test packet_plane_session_expiry_marks_datagram_path_unhealthy`; config validation: `nix develop -c cargo test packet_plane_config`; full replay coverage: `nix develop -c cargo test runtime::forward::tests` |
| Resource limits and rate limits | operational | `src/config.rs`, `src/runtime/p2p.rs`, and `src/runtime/runner.rs` expose swarm connection limits, stream limits, relay limits, and per-peer inbound packet rate limiting. | `nix develop -c cargo test runtime::runner::tests::peer_packet_rate_limiter_caps_each_peer_independently`; full config coverage: `nix develop -c cargo test config::tests` |
| Path selection and promotion | operational | `src/path.rs` scores direct datagram, direct stream, TCP, and relay paths. Runtime records promotions/fallbacks, logs path-selection changes, demotes selected packet-plane datagram paths on send-side transport/session failures so later queue draining can fall back to the next usable stream or relay path, and immediately dials the peer's configured/discovered addresses to recover a direct packet-plane path. | `nix develop -c cargo test path::tests`; demotion: `nix develop -c cargo test packet_plane_transport_failure_demotes_datagram_path_to_relay`; recovery targets: `nix develop -c cargo test packet_plane_recovery_targets` |
| NAT/DCUtR hole punching | operational on supported Linux hosts | The privileged namespace test starts a relay plus two peers, uses relay fallback, enables DCUtR and AutoNAT, observes a successful hole-punch result, promotes to a direct TCP path, negotiates packet-plane sessions only after a direct transport path exists, and verifies TUN traffic over packet-plane datagrams. | `nix run .#tun-e2e -- tun_namespace_relay_overlay_promotes_to_direct_path -- --ignored --exact --nocapture` |
| Discovery and public libp2p/IPFS bootstrap | partial | Private Kademlia, mDNS, configurable bootstrap peers, relay reservations, a `--relay-peer PEER_ID=MULTIADDR` infrastructure shortcut, an opt-in IPFS Kademlia protocol/bootstrap template, libp2p RSA peer identity decoding for legacy public IPFS bootstrap IDs, and a rootless `bootstrap-check` diagnostic exist. Public peers are documented as reachability infrastructure, not membership or route authority. The relay shortcut adds the relay to bootstrap/AutoNAT reachability peers and creates a matching `/p2p-circuit` reservation without adding the relay to the VPN peer list. Bootstrap, configured peer, and relay-reservation addresses are registered as AutoNAT probe servers when AutoNAT is enabled. Membership-protected overlays scope Kademlia provider records by the derived membership tag, while no-membership overlays keep the network-name key for compatibility. During membership-key rotation, refreshes query previous-tag rendezvous keys without advertising old tags. Provider results from public or private DHT routing are counted, but only configured VPN peers become dial candidates; ignored providers are exposed as runtime metrics. `bootstrap-check` builds the same libp2p host without TUN, starts bootstrap/Kademlia/AutoNAT setup, reports per-bootstrap-peer reachability with any/all success thresholds, reports observed AutoNAT status, reports DCUtR enablement/readiness/success counters, reports per-relay reservation acceptance/relayed-listen readiness, reports per-configured-relayed-peer circuit dialing status, and can require configured relay reservations, an observed non-unknown AutoNAT status, rootless DCUtR relay readiness, successful DCUtR hole punching, and configured relayed peer circuits to become operational. Ignored live smokes can validate a supplied public relay and a supplied public-relay DCUtR path through `P2P_VPN_LIVE_RELAY_MULTIADDR`. A live rootless smoke check connected to all five default public IPFS bootstrap peers, registered five AutoNAT probe servers, and observed AutoNAT `private` status. mDNS and DHT namespace tests now verify discovery, capability exchange, packet-plane endpoint advertisement, signed packet-plane session negotiation, and real datagram packet forwarding after discovery. A relay-assisted signed-invite namespace test now proves an imported invite can carry the inviter's relayed address into a runnable overlay that exchanges capabilities and forwards TUN traffic over circuit relay. | `nix develop -c cargo test runtime::p2p::tests::build_node_accepts_ipfs_compatible_kademlia_protocol`; rootless bootstrap/relay/AutoNAT/DCUtR/relayed-peer-circuit diagnostic: `nix develop -c cargo test bootstrap_check`; live public relay smoke harnesses: `P2P_VPN_LIVE_RELAY_MULTIADDR=/dns4/relay.example.net/tcp/4001/p2p/RELAY nix develop -c cargo test runtime::bootstrap_check::tests::bootstrap_check_can_probe_live_public_relayed_peer_circuit -- --ignored --exact --nocapture` and `P2P_VPN_LIVE_RELAY_MULTIADDR=/dns4/relay.example.net/tcp/4001/p2p/RELAY nix develop -c cargo test runtime::bootstrap_check::tests::bootstrap_check_can_probe_live_public_dcutr_success -- --ignored --exact --nocapture`; public IPFS + AutoNAT smoke: `nix develop -c cargo run --quiet -- init-config --output /tmp/p2p-vpn-public/p2p-vpn.json --ipfs-kademlia --ipfs-bootstrap-peers --disable-mdns --disable-kademlia-provider-advertisement --force` then `nix develop -c cargo run --quiet -- bootstrap-check --config /tmp/p2p-vpn-public/p2p-vpn.json --timeout-seconds 45 --require-autonat-status`; relay shortcut: `nix develop -c cargo test relay_peer_shortcut`; rendezvous scoping: `nix develop -c cargo test kademlia_rendezvous_key`; rotation lookup: `nix develop -c cargo test kademlia_refresh_queries_previous_membership_rendezvous_keys`; provider filtering: `nix develop -c cargo test runtime::runner::tests::kademlia_provider_results_dial_configured_peers_and_count_failures`; AutoNAT relay servers: `nix develop -c cargo test runtime::p2p::tests::build_node_registers_relay_reservations_as_autonat_servers`; invite relay E2E: `nix run .#tun-e2e -- tun_namespace_invite_import_crosses_relay_overlay -- --ignored --exact --nocapture`; mDNS E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_mdns_discovered_overlay -- --ignored --exact --nocapture`; DHT E2E: `nix run .#tun-e2e -- tun_namespace_ping_crosses_dht_discovered_overlay -- --ignored --exact --nocapture` |
| MTU and MSS behavior | operational | Effective packet MTU is bounded by the wire payload length, packet-plane UDP envelope ceiling, and peer/path MTU. Packet-plane sealing rejects payloads that would exceed the safe UDP datagram ceiling before socket send. Linux route commands include MTU and `advmss` hints when possible. Oversized path-MTU drops generate local IPv4 fragmentation-needed or ICMPv6 packet-too-big feedback when the original packet is parseable, outbound oversized drops lower the selected path's MTU estimate to the proven negotiated ceiling, and packet-plane path probes exercise the selected datagram path with ACK-backed upward probe search capped by authenticated peer capability and session MTU. Metrics distinguish generic path-MTU updates from ACK-confirmed path-MTU probe raises, expose path-probe ACK sends, and classify packet-too-big feedback as emitted, skipped because no TUN writer is available, skipped because the original packet is unparseable, or failed during write. Configured, live-peer, and daemon MTU views explicitly report that overlay fragmentation is disabled. | `nix develop -c cargo test runtime::tun::tests::packet_too_big_builds_ipv4_fragmentation_needed`; packet-too-big feedback outcomes: `nix develop -c cargo test runtime::runner::tests::packet_too_big_feedback_records_no_writer_and_unparseable_outcomes`; packet-plane ceiling: `nix develop -c cargo test packet_plane_datagram_rejects_payload_above_udp_safe_ceiling`; path MTU learning: `nix develop -c cargo test runtime::runner::tests::drain_outbound_queue_respects_peer_capability_mtu`; policy output: `nix develop -c cargo test mtu_lines_configured_report_route_mss_and_path_estimates`; `nix develop -c cargo test peer_live_mtu_lines_report_negotiated_path_ceiling`; `nix develop -c cargo test runtime_mtu_lines_report_overlay_fragmentation_policy`; path probes: `nix develop -c cargo test path_probe`; full MTU coverage: `nix develop -c cargo test mtu` |
| Daemon lifecycle | operational | `src/runtime/runner.rs` handles shutdown reasons, structured runtime logs, metrics snapshots, control-socket shutdown, and orderly cleanup. | `nix develop -c cargo test runtime::runner::tests::runtime_control_shutdown_acknowledges_and_requests_stop`; full lifecycle coverage: `nix develop -c cargo test runtime::runner::tests` |
| Local daemon control CLI | operational | `src/runtime/control_socket.rs` and `src/main.rs` expose `daemon-status`, `daemon-state`, `daemon-peers`, `daemon-routes`, `daemon-paths`, `daemon-mtu`, `daemon-capabilities`, and `daemon-shutdown`. Status and state output include the configured packet-plane session TTL and replay-window limit alongside listener/session counts. | `nix develop -c cargo test runtime::control_socket::tests::control_socket_serves_daemon_view_requests`; TTL/limit line coverage: `nix develop -c cargo test runtime_state_lines_include_peer_capabilities_paths_and_probes` |
| Remote status/control CLI | operational | `src/runtime/remote.rs` and `src/main.rs` query peers for service status, live paths, routes, capabilities, MTU-related state, reported packet-plane session TTL/replay-window limit, and packet-plane payload/overhead limits without requiring daemon-local socket access. Older service responses without those fields decode as unknown. | `nix develop -c cargo test runtime::remote::tests::query_peer_status_exchanges_live_control_and_service_status`; service compatibility: `nix develop -c cargo test status_response_decodes_missing_packet_plane_limits_as_unknown`; CLI line coverage: `nix develop -c cargo test peer_live` |
| Security audit logging | operational | `src/runtime/runner.rs` emits structured audit fields for rejected packets without logging packet payload bytes. Metrics classify rejection reasons. | `nix develop -c cargo test runtime::runner::tests::packet_rejection_audit_fields_include_safe_packet_metadata` |
| NixOS integration | operational | `nix/nixos-module.nix` defines instances, systemd units, runtime directories, firewall ports, TUN kernel module, control socket shutdown, and restart behavior. | `nix flake check` |
| Release packaging | operational | `flake.nix` builds the binary package and reproducible release archive with README, flake files, NixOS module, examples, and this matrix. | `nix build .#releaseArchive && tar -tzf result` |

## Remaining Non-Trivial Gaps

1. Native QUIC datagrams are not operational through the current libp2p
   dependency surface. Closing this requires a lower-level transport integration,
   a dependency upgrade that exposes application datagrams, or a different
   datagram-capable substrate. Owned packet-plane listener configuration,
   listener binding, endpoint capability advertisement, automatic signed packet
   session negotiation over the control plane, X25519 key agreement, per-peer
   packet-plane session state, encrypted datagram frame primitives, socket-level
   UDP send/receive primitives, daemon outbound queue sends over an established
   packet-plane session, and daemon inbound handling for established
   packet-plane sessions exist.
2. Public libp2p/IPFS infrastructure is supported as discovery and reachability
   assistance, and public bootstrap reachability can now be checked with a
   rootless diagnostic that also covers configured relay reservation acceptance,
   AutoNAT status results, DCUtR relay readiness, configured relayed peer circuit
   dialing, and observed DCUtR success events. Ignored live smokes can validate a
   supplied public relay and DCUtR path with `P2P_VPN_LIVE_RELAY_MULTIADDR`, but
   the project still needs recorded public-network smoke results from known-good
   public infrastructure. It is not, and should not become, membership or route
   authority.
3. Privileged namespace tests prove important operational flows on this host, but
   they still require a Linux kernel with user namespaces, network namespaces,
   veth, and `/dev/net/tun`.
